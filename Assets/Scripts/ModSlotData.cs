using UnityEngine;

public class ModSlotData : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public Modifier modifier; // Reference to the modifier this slot holds
}
